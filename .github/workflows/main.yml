name: Release

on:
  workflow_dispatch:
jobs:
  release:
    uses: jayapraveenasrinivasan0315-afk/cherry-pick/release.yml
    with:
      app_name: frontend
    secrets:
      GH_TOKEN: ${{ secrets.GH_PAT }}

